class Account {
    id: number; 
    name: string;
    amount: number;
    creationDate: Date;
    
    constructor(id: number, name: string, amount: number) {
        this.id = id;
        this.name = name;
        this.number = number;
    }
}

export default Account;

